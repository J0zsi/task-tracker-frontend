import { ScrollToFirstInvalidFormField } from './scroll-to-first-invalid-form-field.directive';

describe('ScrollToFirstInvalidFormField', () => {
  it('should create an instance', () => {
    const directive = new ScrollToFirstInvalidFormField();
    expect(directive).toBeTruthy();
  });
});
